# Mobile Readability Brief

目标：不改中文行程内容，只提升老人端手机阅读体验；后续真实照片和真实地图接入时，继续沿用这套移动端规则。

## 优先级 1：先解决“看得清”

- 把正文基准字号从当前偏紧的 `13px~15px` 提到更适合老人阅读的范围，建议：
  - `.sd, .dm p, .pic .d, .gct p, .stf p, .aid` 统一到 `15px~16px`
  - 行高提升到 `1.75~1.9`
  - 深色文字保持，但弱化灰字对比差异，不要让次级说明过浅
- 标题层级再拉开一点：
  - `h1` 继续保留大标题，但移动端压缩 hero 上下留白
  - `.st`, `.dm h3`, `.gct h3`, `.sc2 h4` 增加字重和间距，避免和正文混在一起
- 段落与列表不要贴边：
  - 每个卡片内外边距至少保留 `16px`
  - 长说明文案改成更短的视觉块，避免一屏塞太多信息

## 优先级 2：再解决“点得中”

- 顶部横向导航按钮太小，建议移动端最小点击区 `44px`：
  - `.sb` 增加左右 padding 和上下 padding
  - 保持横向滚动，但加 `scroll-snap-type: x proximity`
  - 当前页按钮高亮更明显，不只靠底边线
- 章节锚点要避开 sticky 导航遮挡：
  - 给所有大 section 加 `scroll-margin-top`，值参考导航高度再加一点安全边距
  - `scrollIntoView()` 保留平滑滚动，但默认对齐到可见顶部
- 图片和卡片链接区域要更容易触发：
  - 图片卡片、日程卡片头部整体可点，减少用户只点到文字边缘
  - 若加入 POI 点击层，点击热区不要小于 44px

## 优先级 3：最后解决“看不累”

- 现在页面信息密度偏高，建议移动端按顺序减负：
  - `gallery` 改成单列，先看图再看描述
  - `day` 卡片每张只保留最重要的 2-3 条信息，次要提示折叠或后置
  - 住宿区和备选方案区改成更大的分组块，减少连续小卡片带来的视觉疲劳
- 站内滚动节奏要更稳：
  - `.rv` 动画时长保持短一点，避免老人滑动时内容“漂”
  - 关闭过多 hover/阴影变化，手机上只保留轻微层次

## 真实图片接入建议

- 现在的 `assets/*.svg` 只是占位插画，移动端要换成真实照片后，建议统一使用：
  - `img` 统一 `width:100%`、`object-fit:cover`
  - 固定一个接近 4:3 或 16:10 的裁切比例，避免版面抖动
  - 每张图加简短说明，不要只靠图本身表达信息
- 图片优先替换顺序：
  - 霞山古村
  - 钱江源 / 齐溪
  - 高田坑 / 台回山
  - 桐庐 / 芦茨溪谷
- 如果以后把真实地图放进来，建议把地图卡和照片卡分成两个独立模块，不要混在同一屏里抢注意力

## 具体落点

- `index.html`
  - `body`、`.sc`、`.dc`、`.dch`、`.dcb`、`.gg`、`.stc` 的移动端 padding 重排
  - `.sb`、`.nv`、`.nvi`、`scrollIntoView()`、IntersectionObserver 相关的锚点和高亮逻辑微调
  - 新增 `scroll-margin-top`，统一修正 sticky 导航遮挡问题
- 不改动
  - 行程中文内容
  - 日期顺序
  - 住宿建议本身
  - 已有的路线逻辑

## 最小验收标准

- 手机上默认 1 屏能清楚看出当前章节主题
- 顶部导航单个按钮能稳定点中
- 滚动到任意日程标题时不会被顶栏盖住
- 图片区从“占位感”变成“真实景点预览感”
